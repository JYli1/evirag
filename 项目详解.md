# EviRAG 项目详解

本文面向刚学完 Java 基础语法、还没系统接触 Spring Boot / Vue 的同学。目标是帮你按模块看懂这个项目：每个包负责什么、一次请求怎么流动、RAG 问答是怎么完成的。

阅读时先抓住一个核心思路：前端负责页面和用户操作，后端负责业务规则和数据处理，MySQL 保存业务数据，Chroma 保存向量索引，LLM 负责生成回答。

## 1. 项目是什么

EviRAG 是一个中文文档问答系统。用户可以创建知识库、上传文档、等待文档被解析成切片并写入向量库，然后基于知识库提问。系统会先检索相关切片，再把切片和问题一起发给大模型，让大模型基于证据回答。

项目同时支持没有知识库的普通聊天。也就是说，工作台里有两种会话：

- 自由会话：不查知识库，直接把问题和历史对话发给 LLM。
- 知识库会话：先查文档切片，再把引用片段拼到 prompt 里调用 LLM。

## 2. 技术栈总览

后端：

- Java 17：主要开发语言。
- Spring Boot：负责启动 Web 服务、加载配置、管理对象。
- Spring MVC：把 HTTP 请求映射到 Controller 方法。
- Spring Security：处理登录态、JWT 鉴权、管理员权限。
- Spring Data JPA：用 Repository 操作 MySQL。
- Flyway：启动时自动执行数据库建表脚本。
- Java HttpClient：请求 LLM、Embedding、Chroma。
- SseEmitter：后端向前端持续推送流式回答。

前端：

- Vue 3：构建页面组件。
- TypeScript：给前端对象增加类型约束。
- Vue Router：控制登录、注册、工作台、管理员页面路由。
- Pinia：保存登录用户和 token。
- Axios：发送普通 JSON 请求。
- Fetch + ReadableStream：读取 SSE 流式聊天响应。
- marked + DOMPurify：把 Markdown 安全渲染成 HTML。

数据和外部服务：

- MySQL：保存用户、知识库、文档、切片、聊天记录、管理员日志。
- Chroma：保存向量，用来做相似度检索。
- OpenAI-compatible LLM：生成回答。
- OpenAI-compatible Embedding：把文本转成向量。

## 3. 后端整体结构

后端主目录：

```text
backend/src/main/java/com/evirag
```

常见分层可以这样理解：

```text
Controller  接收 HTTP 请求，取参数，返回响应
Service     写业务流程，比如注册、上传、问答、索引
Repository  操作数据库，通常不写复杂业务判断
Entity      对应数据库表，比如 User、Document
DTO/Response 前后端传输对象，避免直接暴露 Entity
Config      Spring 配置，比如安全、异步线程池、环境变量
Client      调外部系统，比如 LLM、Embedding、Chroma
```

主要包说明：

```text
auth        注册、登录、验证码、JWT
user        用户实体和用户数据库访问
knowledge   知识库管理
document    文档上传、删除、切片查询、文档实体
chunk       文本切片规则
retrieval   Chroma 向量库客户端和索引服务
embedding   Embedding 接口客户端
llm         LLM 接口客户端
rag         RAG 编排：改写问题、检索、组 prompt、调用 LLM
chat        会话、消息、SSE 流式问答
admin       管理员面板、用户详情、审计日志
config      Spring 配置和环境变量配置
common      统一响应、异常处理、安全响应格式
```

## 4. Spring 注解怎么理解

如果你只学过 Java 基础，看到 Spring 注解可能会陌生。先记住这些常用注解：

- `@SpringBootApplication`：项目入口，启动 Spring Boot。
- `@RestController`：这个类是 HTTP 接口入口。
- `@RequestMapping` / `@GetMapping` / `@PostMapping`：声明接口路径和请求方式。
- `@Service`：这个类是业务服务，由 Spring 自动创建对象。
- `@Component`：普通组件，也由 Spring 自动创建对象。
- `@Repository`：数据库访问接口，一般继承 JPA Repository。
- `@Entity`：这个类对应数据库表。
- `@Transactional`：方法内数据库操作放在事务里，失败时可以回滚。
- `@AuthenticationPrincipal`：从 JWT 登录态里取当前用户。
- `@Valid`：让请求参数按 DTO 上的校验规则检查。

Spring 的一个重要思想是“对象由框架管理”。例如 `ChatService` 构造函数里需要 `RagService`、`LlmClient`，不需要自己 `new`，Spring 会自动把对象传进来。

## 5. 启动入口和配置

启动入口：

```text
backend/src/main/java/com/evirag/EviRagApplication.java
```

项目启动时先调用 `DotenvBootstrap.load()`，目的是读取 `backend/.env` 中的环境变量。数据库密码、JWT 密钥、邮箱授权码、LLM Key 这些敏感信息不应该写死在代码里。

主配置文件：

```text
backend/src/main/resources/application.yml
```

这里使用 `${变量名:默认值}` 读取环境变量。例如：

```yaml
evirag:
  llm:
    base-url: ${LLM_BASE_URL:https://api.openai.com/v1}
```

意思是：如果 `.env` 里配置了 `LLM_BASE_URL`，就用 `.env` 的值；否则使用默认值。

配置类：

```text
backend/src/main/java/com/evirag/config/AppProperties.java
```

它把 `application.yml` 中的 `evirag.*` 配置映射成 Java 对象，后面 `LlmClient`、`EmbeddingClient`、`ChromaClient` 都从这里读取配置。

## 6. 数据库和实体

数据库表由 Flyway 脚本创建：

```text
backend/src/main/resources/db/migration
```

核心实体：

```text
User              用户
EmailVerificationCode 邮箱验证码
KnowledgeBase     知识库
Document          上传文档
DocumentChunk     文档切片
ChatSession       聊天会话
ChatMessage       聊天消息
AdminAuditLog     管理员操作日志
```

一个常见关系是：

```text
一个用户 -> 多个知识库
一个知识库 -> 多个文档
一个文档 -> 多个切片
一个用户 -> 多个会话
一个会话 -> 多条消息
```

MySQL 保存的是业务主数据。即使 Chroma 里也有切片文本和 metadata，仍然以 MySQL 的 `documents`、`document_chunks` 为准。Chroma 只是为了快速检索相似切片。

## 7. 统一响应和异常

普通接口返回：

```text
ApiResponse<T>
```

成功时大致结构是：

```json
{
  "success": true,
  "data": {}
}
```

异常处理在：

```text
backend/src/main/java/com/evirag/common/exception/GlobalExceptionHandler.java
```

Controller 不需要每个方法都写 `try/catch`。业务层抛出异常后，全局异常处理器会把它转换成统一 JSON 错误。

## 8. 登录和鉴权流程

认证入口：

```text
backend/src/main/java/com/evirag/auth/AuthController.java
backend/src/main/java/com/evirag/auth/AuthService.java
```

注册流程：

```text
前端提交邮箱 -> 后端发送验证码 -> 用户提交邮箱、密码、验证码
-> AuthService 校验验证码
-> 密码用 BCrypt 加密
-> 保存 User
-> JwtService 生成 token
-> 前端保存 token
```

登录流程：

```text
前端提交邮箱和密码
-> AuthService 查用户
-> BCrypt 校验密码
-> JwtService 生成 token
-> 前端后续请求带 Authorization: Bearer token
```

鉴权配置：

```text
backend/src/main/java/com/evirag/config/SecurityConfig.java
```

重要规则：

- `/api/auth/**` 中的注册、登录、验证码接口允许匿名访问。
- `/api/admin/**` 必须是管理员角色。
- 其他接口必须登录。
- 项目不使用后端 Session，而是使用 JWT。

JWT 服务：

```text
backend/src/main/java/com/evirag/auth/JwtService.java
```

它负责生成和解析 token。启动时会拒绝空的、默认的、过短的 JWT 密钥，所以 `.env` 里必须配置安全的 `JWT_SECRET`。

## 9. 知识库流程

知识库接口：

```text
backend/src/main/java/com/evirag/knowledge/KnowledgeBaseController.java
backend/src/main/java/com/evirag/knowledge/KnowledgeBaseService.java
```

前端创建知识库时，只传知识库名称等业务信息，不传 `userId`。后端从 JWT 中读取当前用户 ID，这样普通用户不能冒充别人创建或读取数据。

每个知识库会生成一个 Chroma collection 名称。后续文档切片会写入这个 collection。

## 10. 文档上传到索引流程

文档入口：

```text
backend/src/main/java/com/evirag/document/DocumentController.java
backend/src/main/java/com/evirag/document/DocumentService.java
```

向量索引：

```text
backend/src/main/java/com/evirag/retrieval/VectorIndexService.java
```

一次上传的核心流程：

```text
1. 前端选择文件并上传到 /api/knowledge-bases/{id}/documents
2. DocumentService 校验知识库属于当前用户
3. 文件保存到本地 uploads 目录
4. MySQL 新增 Document，状态为 PROCESSING
5. 后端异步调用 VectorIndexService.indexDocument
6. 根据文件类型选择解析器
7. ChunkService 把文本切成多个 TextChunk
8. 保存 DocumentChunk 到 MySQL
9. EmbeddingClient 把切片文本转成向量
10. ChromaClient 创建或复用 collection
11. ChromaClient 把向量 upsert 到 Chroma
12. Document 状态改为 READY，记录 chunkCount
```

如果解析失败、文本为空、Embedding 模型名错误、Chroma 不可用，文档会变成 `FAILED`，并保存错误阶段和错误摘要。前端会显示失败状态，用户可以删除失败文档。

解析器位置：

```text
backend/src/main/java/com/evirag/document/parser
```

当前支持：

- PDF
- DOCX
- TXT
- Markdown

## 11. 切片和向量

切片服务：

```text
backend/src/main/java/com/evirag/chunk/ChunkService.java
```

切片的作用是把长文档拆成较短文本。大模型和向量检索都不适合直接处理整篇长文档。项目默认使用：

```text
CHUNK_MAX_CHARS=1200
CHUNK_OVERLAP_CHARS=120
```

`overlap` 表示相邻切片之间保留一点重复内容，降低句子或段落被切断后丢失上下文的概率。

Embedding 客户端：

```text
backend/src/main/java/com/evirag/embedding/OpenAiCompatibleEmbeddingClient.java
```

它调用 `/embeddings` 接口，把文本数组变成向量数组。这里的 `EMBEDDING_MODEL` 必须是 embedding 模型，不能填聊天模型。

## 12. Chroma 的作用

Chroma 客户端：

```text
backend/src/main/java/com/evirag/retrieval/ChromaClient.java
```

它负责四件事：

- `ensureCollection`：创建或复用 collection。
- `upsert`：写入切片向量。
- `query`：根据问题向量查询相似切片。
- `deleteByDocumentId`：删除某个文档对应的向量。

检索时会带 `where` 条件：

```text
user_id = 当前用户
knowledge_base_id = 当前知识库
```

这样可以避免不同用户、不同知识库的切片混在一起。

## 13. RAG 问答流程

聊天入口：

```text
backend/src/main/java/com/evirag/chat/ChatController.java
backend/src/main/java/com/evirag/chat/ChatService.java
```

RAG 编排：

```text
backend/src/main/java/com/evirag/rag/RagService.java
```

一次知识库问答的流程：

```text
1. 前端 POST /api/sessions/{sessionId}/messages/stream
2. ChatService 确认会话属于当前用户
3. 保存用户问题
4. 读取最近几轮历史消息
5. 检查会话是否绑定知识库
6. 检查知识库是否已有可检索切片
7. RagService 改写问题
8. EmbeddingClient 把问题转成向量
9. ChromaClient 检索 Top-K 切片
10. RagService 把问题和引用片段拼成 prompt
11. LlmClient 流式请求大模型
12. 后端把 answer_delta 一段段推给前端
13. 回答结束后保存助手消息
14. 发送 answer_done 事件
```

如果会话没有知识库，系统会跳过 embedding 和 Chroma，直接调用 LLM。这样用户没上传文档时也能聊天。

如果知识库没有切片，系统也不会强行查 Chroma，而是返回“当前知识库中还没有可检索内容”的提示，并走普通 LLM 回答。

## 14. Prompt 是怎么组织的

RAG prompt 在：

```text
backend/src/main/java/com/evirag/rag/RagService.java
```

它主要由两部分组成：

```text
system 消息：告诉模型只能基于引用片段回答，不要编造来源。
user 消息：放用户问题和检索到的引用片段。
```

自由问答 prompt 在：

```text
backend/src/main/java/com/evirag/chat/ChatService.java
```

它告诉模型当前不使用知识库引用，直接回答用户问题。

对初学者来说，可以把 prompt 理解为“发给模型的输入文本”。区别是这个输入由后端根据业务场景自动组装，而不是用户直接写完整内容。

## 15. LLM 流式输出

LLM 客户端：

```text
backend/src/main/java/com/evirag/llm/OpenAiCompatibleLlmClient.java
```

它调用 OpenAI-compatible 的：

```text
POST /chat/completions
```

流式模式下，大模型不会一次性返回完整答案，而是不断返回很多小片段。后端把每个片段作为 `answer_delta` SSE 事件发给前端。

前端收到后，会把最新助手消息的内容不断追加，所以页面上能看到打字机效果。

## 16. SSE 事件

后端使用：

```text
org.springframework.web.servlet.mvc.method.annotation.SseEmitter
```

前端使用：

```text
frontend/src/api/chat.ts
```

主要事件：

```text
retrieval_start   开始检索
retrieval_done    检索完成，携带引用片段
debug_log         调试日志，如 LLM 请求摘要、响应摘要
answer_delta      回答增量片段
answer_done       回答完成，携带完整答案和引用
error             出错
```

普通 JSON 请求可以用 axios，但 SSE 要边收到边读取，所以前端聊天流使用 `fetch` 和 `ReadableStream`。

## 17. 前端页面结构

前端主目录：

```text
frontend/src
```

主要目录：

```text
api          调后端接口
components   可复用组件
pages        页面级组件
router       路由
stores       Pinia 状态
styles       全局样式变量
assets       logo 等资源
```

路由文件：

```text
frontend/src/router/index.ts
```

页面：

```text
/login            登录
/register         注册
/reset-password   找回密码
/workbench        工作台
/admin            管理员面板
```

路由守卫会检查登录状态。没有登录不能进入工作台；不是管理员不能进入管理员面板。

## 18. 工作台页面

工作台页面：

```text
frontend/src/pages/workbench/WorkbenchPage.vue
```

它负责组合这些功能：

- 当前用户信息卡片。
- 知识库列表。
- 文档上传和文档状态。
- 文档切片预览。
- 会话列表。
- 消息列表。
- 提问输入框。
- 引用证据面板。
- 过程日志面板。

核心状态包括：

```text
knowledgeBases     当前用户的知识库列表
documents          当前知识库下的文档
sessions           当前会话列表
messages           当前会话消息
selectedKbId       当前选中的知识库
selectedSessionId  当前选中的会话
citations          当前回答的引用片段
processLogs        当前过程日志
```

发送消息时，前端会先插入一条 `pending` 的助手消息。收到 `answer_delta` 时，只更新这条最新消息。历史消息不会重新做打字机效果，避免长对话刷新时全部重新动画。

## 19. Markdown 和打字机

消息列表：

```text
frontend/src/components/chat/MessageList.vue
```

这里做了两件事：

- Markdown 渲染：`marked` 把 Markdown 转成 HTML。
- 安全清洗：`DOMPurify` 清洗 HTML，降低 `v-html` 的风险。

打字机效果只作用于最新的 pending 助手消息。已经保存的历史消息会直接渲染，不再逐字动画。

## 20. 文档组件

文档上传组件：

```text
frontend/src/components/document/DocumentUploader.vue
```

它负责：

- 上传文件。
- 显示处理状态。
- 删除文档。
- 预览切片。
- 显示失败原因。

文档状态来自后端：

```text
PROCESSING  处理中
READY       已就绪
FAILED      失败
```

上传后文档索引是异步执行的，所以页面需要轮询刷新状态。看到后端日志“索引完成”后，如果前端仍显示处理中，通常要检查文档状态是否成功更新、前端是否重新拉取列表。

## 21. 管理员面板

管理员页面：

```text
frontend/src/pages/admin/AdminDashboardPage.vue
```

后端服务：

```text
backend/src/main/java/com/evirag/admin/AdminDashboardService.java
```

管理员能看到：

- 总用户数、活跃用户数、禁用用户数。
- 总知识库数、总文档数、成功/失败文档数。
- 问答次数。
- 今日上传数。
- token 估算。
- 配置缺失数量。
- 用户列表。
- 单个用户详情。
- 管理员操作审计日志。

单个用户详情包含：

- 该用户知识库数量。
- 文档数量。
- 切片数量。
- 用户问题数和助手消息数。
- 文档 token 估算。
- 聊天 token 估算。
- 最近文档和最近消息。

## 22. 常见错误怎么看

### JWT 密钥错误

```text
JWT 密钥不能使用空值、默认值或过短值
```

原因通常是 `backend/.env` 里没有配置 `JWT_SECRET`，或仍然使用 `change-me`。修改后重启后端。

### Embedding 模型不存在

```text
EmbeddingException: HTTP 400
Model does not exist
```

重点检查：

- `EMBEDDING_BASE_URL` 是否是正确服务商。
- `EMBEDDING_MODEL` 是否是 embedding 模型。
- 不要把聊天模型填到 embedding 配置里。

### LLM 连接失败

```text
LLM ConnectException
```

重点检查：

- `LLM_BASE_URL` 是否可访问。
- 网络、代理、DNS、防火墙是否正常。
- `LLM_BASE_URL` 和 `LLM_MODEL` 是否属于同一个服务商。

### Chroma collection 不存在

```text
Collection does not exist
```

常见原因：

- 文档还没成功入库。
- Chroma 数据目录被清空。
- 知识库 collection 名称变了。

当前代码已经在空知识库场景下尽量避免把 Chroma 404 直接暴露给用户。

### Chroma where 条件错误

```text
Invalid where clause
```

要看 `RagService.retrieve` 中传给 Chroma 的 `where` 结构。Chroma v2 对过滤条件格式比较严格，项目中使用 `$and` 同时限制 `user_id` 和 `knowledge_base_id`。

## 23. 推荐读代码顺序

如果你还没接触 Spring，不建议从所有文件一起看。按这个顺序读更稳：

1. `EviRagApplication.java`：先看项目怎么启动。
2. `application.yml` 和 `AppProperties.java`：理解配置从哪里来。
3. `AuthController.java`、`AuthService.java`、`JwtService.java`：理解登录和 token。
4. `SecurityConfig.java`：理解哪些接口要登录、哪些接口要管理员。
5. `KnowledgeBaseController.java`、`KnowledgeBaseService.java`：理解知识库归属。
6. `DocumentController.java`、`DocumentService.java`：理解上传文档。
7. `VectorIndexService.java`、`ChunkService.java`、`OpenAiCompatibleEmbeddingClient.java`、`ChromaClient.java`：理解文档如何进入向量库。
8. `ChatController.java`、`ChatService.java`：理解聊天入口和 SSE。
9. `RagService.java`、`OpenAiCompatibleLlmClient.java`：理解 RAG 和 LLM 请求。
10. `WorkbenchPage.vue`、`chat.ts`、`MessageList.vue`：理解前端如何发请求、收流式回答、渲染消息。
11. `AdminController.java`、`AdminDashboardService.java`、`AdminDashboardPage.vue`：理解管理员页面。

## 24. 初学者看代码时的重点

先不要纠结每个库的全部细节。你只要先看懂这几个问题：

- 请求从哪个 Controller 进来？
- Controller 调用了哪个 Service？
- Service 读写了哪些 Repository？
- 这个流程有没有调用外部服务？
- 数据最后保存到了 MySQL、Chroma，还是只返回给前端？
- 前端哪个 API 函数调用了这个接口？
- 页面哪个组件使用了这个 API 函数？

能回答这些问题，就已经能看懂项目主链路了。后面再逐步补 Spring Security、JPA、SSE、Vue 响应式这些细节。

## 25. 一句话总结

EviRAG 的核心链路是：

```text
上传文档 -> 解析切片 -> embedding 成向量 -> 写入 Chroma
用户提问 -> 问题向量化 -> Chroma 检索切片 -> 拼 prompt -> LLM 流式回答 -> 前端展示答案和证据
```

只要抓住这条线，项目里大多数类都能找到自己的位置。
